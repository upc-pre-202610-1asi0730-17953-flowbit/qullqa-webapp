/**
 * Enumeration of the supported product statuses within the
 * Product & Inventory Management bounded context.
 *
 * @enum {string}
 */
export const ProductStatus = Object.freeze({
    ACTIVE:   'ACTIVE',
    INACTIVE: 'INACTIVE'
});